{
  "name": "full-stack-todo",
  "version": "1.0.0",
  "description": "Practicing live data update from backend without reload the full page",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd todos && npm install && npm run build"
  },
  "author": "Md Niamul Hakim Shawn",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongoose": "^6.5.3"
  }
}
